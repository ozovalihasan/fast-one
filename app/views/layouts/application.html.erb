<!DOCTYPE html>
<html>
  <head>
    <title>Street</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application',  media: 'all', 'data-turbo-track': 'reload' %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application', 'data-turbo-track': 'reload'  %>
  </head>

  <body class="dark">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div id="alert">    </div>
    <div id="notice" class="fixed inset-x-0 bottom-0 flex flex-col items-end justify-right px-4 py-6 sm:p-6 justify-end z-30 pointer-events-none"> </div>
    <%= render 'layouts/navbar' %>
    <%= yield %>
  </body>
</html>
