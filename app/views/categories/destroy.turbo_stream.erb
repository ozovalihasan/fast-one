<%= turbo_stream.append "notice" do %>
    <%= render partial: "layouts/alert", locals: { information: "#{@category.product.name} is removed." } %>
<% end %>

<%= turbo_stream.remove dom_id @category %>
